- Hello ${data.name}!
style
    ---
    .msg {
        color: blue;
        font-size: 16px;
        font-weight: bold;
    }
    ---
p
    <!-- Disable HTMl escaping using $! -->
    - $!{data.htmlMessage}
